spring:
  datasource:
    url: jdbc:postgresql://auth_postgres:5432/auth_postgres
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  kafka:
    bootstrap-servers: kafka:9092

eureka:
  client:
    service-url:
      defaultZone: http://discovery-server:8761/eureka

